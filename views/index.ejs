<!DOCTYPE html>
<html>
  <head>
    <title>Taco Recipes</title>
    <link rel="stylesheet" type="text/css" href="/styles/main.css" />
  </head>

  <body>
    <% if(locals.chosenReciepe){ %> <% console.log(chosenReciepe.Ingredients) %>
    <h1><%= chosenReciepe.name %> 🌮</h1>
    <% } %>

    <form action="/recipe" method="POST" class="buttons">
      <button type="submit" value="chicken" name="choice">🍗</button>
      <button type="submit" value="beef" name="choice">🥩</button>
      <button type="submit" value="fish" name="choice">🐟</button>
    </form>
    <div id="recipeContainer" class="hidden">
      <%if(!locals.chosenReciepe){ %>
      <!-- If no recipe yet, show: -->
      <h2>Pick your favourite taco ingredient 👆</h2>
      <% }else{ %>
      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li>
          <%= chosenReciepe.ingredients.protein.name %>, <%=
          chosenReciepe.ingredients.protein.preparation %>
        </li>
        <li><%= chosenReciepe.ingredients.salsa.name %></li>
        <% chosenReciepe.ingredients.toppings.forEach(element => { %>
        <li><%= element.quantity %> <%= element.name %></li>
        <% }) %>
      </ul>
      <% } %>

      <!-- Else, show the recipe name e.g. <h2>Recipe name</h2>-->
    </div>
  </body>
</html>
